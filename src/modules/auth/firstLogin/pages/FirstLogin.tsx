import React from 'react';
import FirstLoginDesktop from '../components/FirstLoginDeskTop';

interface Props {}
const FirstLogin = (props: Props) => {
  return <FirstLoginDesktop />;
};

export default FirstLogin;
