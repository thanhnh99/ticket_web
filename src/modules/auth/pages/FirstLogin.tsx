import React from 'react';
import FirstLoginDesktop from '../components/firstLogin/FirstLoginDeskTop';

interface Props {}
const FirstLogin = (props: Props) => {
  return <FirstLoginDesktop />;
};

export default FirstLogin;
